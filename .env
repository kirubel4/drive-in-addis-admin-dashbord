# Base URL of the NestJS API
VITE_API_URL=http://localhost:3000

# Set to "false" once your backend endpoints (roads/segments/zones, stats, etc.)
# are ready. While true, the app runs entirely on realistic mock data.
VITE_MOCK_API=true
